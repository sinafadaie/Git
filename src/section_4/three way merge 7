when we have divergence on two branches,
we have a trangle!

1.top of the branch 1
2.top of the branch 2
3.the base (the commit that two branch were start deviation)

it means there are not any direct pass from top of the branch1 to branch 2
so git has to create new commit as merge commit to connect them
this is called three way merged.
no ff merge is kind of three way merge!

                            * (commit-massage)
                            |\
 *             merge        * \
 |  *       ----------->    |  *
 * /                        * /
 |/                         |/
 * (base commit)            * (base commit)


Note: git when want to merge them,
      compare two branches by "base commit"
      and find the differences.

Note: we have conflict, if we change something on both branch
      from base commit!
      (if both changes are exactly same, there are not any conflict!)

Note: when we have not any divergence, and fast forward merge is possible
      we can not have any conflict!
      because we have just ine road!
      there is not two different road!
      conflict is just happened on three way merges!

Note: the divergence happened, when some backward branch p
      have some new commit.
      we have main branch,
      then we create new branch as test,
      we create new commit on test           (we have not any divergence now)
      we create another new commit on test   (we have not any divergence yet)
                                              it's because master branch is history of test branch now
      we create new commit on master
      we have divergence now,
      this commit is not history of test branch now!