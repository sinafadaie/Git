in cherry-pick we transfer entire commit,
but what if we change multiple file on a single commit,
and we just want to transfer just a specific file!

----------------------------------

we create a new branch as "pick-file"
and write all these lines on this branch. (lines after ------)
we want transfer this lines to main branch without merge

but we add some extra file as "pick file 15.2"
on this commit on "pick-file" branch
(this commit contain current file changes and adding "pick file 2")

if we use cherry-pick, we will transfer "pick file 2"
that we don't want him on main!

we make commit here (before -------- )
----------------------------------
