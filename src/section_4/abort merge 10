when we have conflict on merge,
git are in "merge in progress" state!
it means we have "conflict marker" on files,
our repository and working directory is not normal.

if we can not resolve conflicts, we should go back to normal state:

"git merge abort"