we create this file on "bugfix/photo-upload" branch
imagine these lines will solve this bugs!
I create a commit now with "first commit" as massage.

then I create another commit here as "second commit"
so we have some dirty commits now on "bugfix/photo-upload" branch.
we don't want to move them as a history of main by merging!
we aggregate these two commit by squash merging!

now we have been merged this file to main
we have two pre commit as a single commit (e674b72)
these two commit was deleted by garbage collector
after we delete bugfix/photo-upload branch.